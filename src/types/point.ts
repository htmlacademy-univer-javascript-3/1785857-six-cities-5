type PointType = {
  latitude: number;
  longitude: number;
  zoom: number;
}

type PointTitleType = {
  title: string;
}

type PointsType = PointType[];

export type { PointType, PointsType, PointTitleType };
